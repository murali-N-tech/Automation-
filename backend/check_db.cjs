const mongoose = require('mongoose'); const Job = require('./models/Job'); const Resume = require('./models/Resume'); const Application = require('./models/Application'); mongoose.connect('mongodb://localhost:27017/ai-placement').then(async () => { const jobs = await Job.find(); console.log('Jobs:', jobs.length); const resumes = await Resume.find(); console.log('Resumes:', resumes.length); if(resumes.length) console.log('Skills:', resumes[resumes.length-1].parsedData?.skills); const apps = await Application.find(); console.log('Apps:', apps.length); process.exit(0); }).catch(console.error);
